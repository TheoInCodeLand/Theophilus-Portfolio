<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Manage Experience</title>
    <link rel="stylesheet" href="/css/Admin.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body>
    <div class="admin-container">
        <header class="page-header">
            <h2>Work Experience</h2>
            <div>
                <a href="/admin/dashboard" class="action-btn" style="margin-right: 1rem; color: #94a3b8;">Back</a>
                <a href="/admin/experience/add" class="btn-primary action-btn">+ Add New Job</a>
            </div>
        </header>

        <div class="card">
            <table class="data-table">
                <thead>
                    <tr>
                        <th>Role & Company</th>
                        <th>Location</th>
                        <th>Duration</th>
                        <th>Status</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    <% if (experience.length === 0) { %>
                        <tr>
                            <td colspan="5" style="text-align: center; color: #94a3b8; padding: 2rem;">
                                No experience listed yet. Click "Add New Job" to start.
                            </td>
                        </tr>
                    <% } else { %>
                        <% experience.forEach(job => { %>
                        <tr>
                            <td>
                                <strong><%= job.position %></strong><br>
                                <small style="color: #94a3b8;"><%= job.company_name %></small>
                            </td>
                            <td><%= job.location %></td>
                            <td>
                                <%= new Date(job.start_date).toLocaleDateString('en-US', { month: 'short', year: 'numeric' }) %> 
                                - 
                                <% if (job.is_current) { %>
                                    <span style="color: #4ade80; font-weight: bold;">Present</span>
                                <% } else { %>
                                    <%= new Date(job.end_date).toLocaleDateString('en-US', { month: 'short', year: 'numeric' }) %>
                                <% } %>
                            </td>
                            <td>
                                <% if (job.is_current) { %>
                                    <span class="tech-tag" style="background: #064e3b; color: #6ee7b7;">Current</span>
                                <% } else { %>
                                    <span class="tech-tag">Past</span>
                                <% } %>
                            </td>
                            <td>
                                <a href="/admin/experience/edit/<%= job.id %>" class="action-btn btn-edit" title="Edit">
                                    <i class="fa-solid fa-pen"></i>
                                </a>
                                <form action="/admin/experience/delete/<%= job.id %>" method="POST" style="display:inline;" onsubmit="return confirm('Delete this job entry?');">
                                    <button type="submit" class="action-btn btn-danger" title="Delete">
                                        <i class="fa-solid fa-trash"></i>
                                    </button>
                                </form>
                            </td>
                        </tr>
                        <% }) %>
                    <% } %>
                </tbody>
            </table>
        </div>
    </div>
</body>
</html>